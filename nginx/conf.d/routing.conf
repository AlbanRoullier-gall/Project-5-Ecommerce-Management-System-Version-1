# Configuration de routing pour production
# Routing par domaine : monsite.com et admin.monsite.com

# Zone de rate limiting : 200 requêtes / 15 minutes par IP
# Format: 200 requêtes en 900 secondes = ~0.22 r/s, on utilise 13r/m (proche de 200/15)
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=13r/m;

# Map pour WebSocket connections
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# Pas de variables map globales - on utilisera 'set' dans chaque location
# pour forcer la résolution DNS dynamique à chaque requête

# Server block pour monsite.com (Frontend) et domaines Railway
server {
    listen 80;
    server_name monsite.com www.monsite.com _;

    # Headers pour transmettre l'IP réelle
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Health check sans rate limiting
    location /api/health {
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 30s;
        proxy_send_timeout 10s;
        proxy_read_timeout 10s;
    }

    # Routes pour les images uploadées (sans rate limiting)
    location /uploads/ {
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Routes API vers API Gateway
    # L'API Gateway gère les requêtes OPTIONS (preflight CORS) via son middleware CORS
    location /api/ {
        # Rate limiting pour toutes les méthodes (y compris OPTIONS)
        limit_req zone=api_limit burst=50 nodelay;
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        
        # Headers proxy essentiels
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Headers pour CORS et authentification
        proxy_set_header Origin $http_origin;
        proxy_set_header Cookie $http_cookie;
        
        # Headers pour WebSocket (si nécessaire)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts pour éviter les 502
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        
        # Gestion des erreurs
        proxy_next_upstream error timeout invalid_header http_500 http_502 http_503;
        proxy_next_upstream_tries 2;
        proxy_next_upstream_timeout 10s;
        
        # Buffer settings
        proxy_buffering off;
        proxy_request_buffering off;
        
        # Laisser passer les headers CORS de l'API Gateway (ne pas les cacher)
    }

    # Routes Backoffice (chemin /admin) - Pour Railway avec un seul domaine
    # Doit être AVANT location / pour être prioritaire
    location /admin/_next/ {
        rewrite ^/admin/_next/(.*)$ /_next/$1 break;
        set $backend "http://${BACKOFFICE_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
    
    location /admin/ {
        rewrite ^/admin(.*)$ $1 break;
        set $backend "http://${BACKOFFICE_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
    
    location = /admin {
        return 301 /admin/;
    }
    
    location = /auth {
        return 301 /admin/auth/login;
    }
    
    location /auth/ {
        rewrite ^/auth(.*)$ /admin/auth$1 permanent;
    }

    # Toutes les autres routes vers Frontend Next.js
    location / {
        set $backend "http://${FRONTEND_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
}

# Server block pour admin.monsite.com (Backoffice)
# Note: Pour Railway avec un seul domaine, utilisez le routing par chemin dans le premier server block
server {
    listen 80;
    server_name admin.monsite.com;

    # Headers pour transmettre l'IP réelle
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Health check sans rate limiting
    location /api/health {
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 30s;
        proxy_send_timeout 10s;
        proxy_read_timeout 10s;
    }

    # Routes pour les images uploadées (sans rate limiting)
    location /uploads/ {
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Routes API vers API Gateway
    # L'API Gateway gère les requêtes OPTIONS (preflight CORS) via son middleware CORS
    location /api/ {
        # Rate limiting pour toutes les méthodes (y compris OPTIONS)
        limit_req zone=api_limit burst=50 nodelay;
        set $backend "http://${API_GATEWAY_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        
        # Headers proxy essentiels
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Headers pour CORS et authentification
        proxy_set_header Origin $http_origin;
        proxy_set_header Cookie $http_cookie;
        
        # Headers pour WebSocket (si nécessaire)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts pour éviter les 502
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        
        # Gestion des erreurs
        proxy_next_upstream error timeout invalid_header http_500 http_502 http_503;
        proxy_next_upstream_tries 2;
        proxy_next_upstream_timeout 10s;
        
        # Buffer settings
        proxy_buffering off;
        proxy_request_buffering off;
        
        # Laisser passer les headers CORS de l'API Gateway (ne pas les cacher)
    }
    

    # Toutes les autres routes vers Backoffice Next.js
    location / {
        set $backend "http://${BACKOFFICE_HOST}";
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
}
